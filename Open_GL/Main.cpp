//#include "glew_and_freeglut.h" only one of them can work at the moment, not both
#include "glfw_test.h"
int main(int argc, char **argv)
{
	//start_window(argc, argv);

	glfw_window(argc, argv);

	return 0;
}